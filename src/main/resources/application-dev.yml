server:
  port: 8080
spring:
  config:
    activate:
      on-profile: dev
openapi:
    service:
      api-docs: api-service
      server: http://localhost:${server.port}
      title: API Document
      version: 1.0.0
      description: Mo ta API Moi Truong Dev

