server:
  port: 8081
spring:
  config:
    activate:
      on-profile: test
openapi:
  service:
    api-docs: api-service
    server: http://localhost:${server.port}
    title: API Document
    version: 1.0.0
    description: Mo ta API Moi Truong Test
